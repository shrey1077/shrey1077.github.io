# src/styles

Reserved for future, phase-specific stylesheets (CSS modules or global partials)
that are larger than a component-level concern — e.g. a scroll-narrative
section's layout in Phase 3.

Phase 1 intentionally keeps all styling in `src/app/globals.css` plus a handful
of Tailwind utility classes, so this directory is currently empty by design.
